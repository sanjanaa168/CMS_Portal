# Models Layer (Domain Entities)

This folder will contain your database entity classes representing core domain models (e.g., `Complaint.cs`, `User.cs`, `Category.cs`, `StatusEnum.cs`).

- Each class maps directly to a table or value object in your PostgreSQL database.
- Decorated with validation attributes or configured via EF Core Fluent API.
