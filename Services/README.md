# Services Layer (Business Logic)

This folder contains service interfaces and implementation classes that encapsulate core business logic and rules.

- **Why Services?** Keeping business logic out of controllers adheres to the Single Responsibility Principle (SRP) and makes unit testing straightforward.
- Example future files: `IComplaintService.cs`, `ComplaintService.cs`.
