# DTOs Layer (Data Transfer Objects)

This folder contains request and response models used to transfer data between the client (such as a React frontend) and the API controllers.

- **Why DTOs?** They prevent over-posting vulnerabilities and decouple API contracts from the internal database schema.
- Example future files: `CreateComplaintDto.cs`, `ComplaintResponseDto.cs`, `UpdateStatusDto.cs`.
